/**
 * Input
 *
 * Styled form input with built-in label and validation. Supports:
 * - Text, email, password, textarea types
 * - Floating or stacked label
 * - Error state with error message display
 * - Success state (valid input)
 * - Disabled state
 *
 * Props: type, label, value, onChange, error, required, placeholder
 */
